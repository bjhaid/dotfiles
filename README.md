# dotfiles

run `./bootstrap.sh` to bootstrap the configuration here.
the script self-installs `ansible` when needed, then applies `ansible/bootstrap.yml`.
run `./bootstrap.sh --dry-run` to preview changes with ansible check mode.
